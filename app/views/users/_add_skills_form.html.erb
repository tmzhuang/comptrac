<%= form_tag(@user, method: "get") do %>
  <%= select_tag '@skills[name][]',
    options_from_collection_for_select(Skill.all, "name", "name"),
    { :multiple => true, :size =>10 }
  %>
  <%= submit_tag("Add Skills") %>
<% end %>
