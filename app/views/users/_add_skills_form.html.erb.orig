<<<<<<< HEAD
<%= form_for(@user) do |f| %>
  <%= fields_for(@user_skills) do |us| %>
    <div class="field">
      <%= collection_select(:skills, :ids, @all_skills, :id, :name, {}, { multiple: true }) %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
=======

<%= form_for @user do |f| %>
<%= f.select :skill_ids, Skill.all.collect {|x| [x.name, x.id]}, {}, :multiple => true, :size =>10 %>
  <div class="actions">
    <%= f.submit %>
  </div>

>>>>>>> origin/milestone-1
<% end %>


